{{ knp_pagination_sortable(articles, 'Date', 'a.createdAt', {'direction': 'desc', 'class': 'btn btn-primary d-flex gap-1'}) }}
{{ knp_pagination_sortable(articles, 'Title', 'a.title', {'direction': 'desc', 'class': 'btn btn-primary d-flex gap-1'}) }}